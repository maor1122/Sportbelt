<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background color or drawable for the button -->
    <item android:drawable="@android:color/transparent" />

    <!-- Drawable at the top with desired size -->
    <item
        android:drawable="@drawable/baseline_trending_up_24"
        android:gravity="top|center_horizontal"
        android:width="80dp"
        android:height="80dp"
        />
</layer-list>